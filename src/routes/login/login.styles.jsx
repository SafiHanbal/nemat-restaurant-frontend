import styled from 'styled-components';
import { Link } from 'react-router-dom';

export const ResetPassLink = styled(Link)`
  display: inline-block;
  margin-top: 15px;
  color: var(--color-primary);
  cursor: pointer;
`;
